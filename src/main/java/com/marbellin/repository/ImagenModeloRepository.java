package com.marbellin.repository;

import com.marbellin.entity.ImagenModelo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ImagenModeloRepository extends JpaRepository<ImagenModelo, Long> {
}
